<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="This is a webPage for Dummy Data Generator and that data is stored in out MonogoDB collection."
    />
    <title>Dummy Data Generator</title>
    <style>
      * {
        background-color: azure;
        font-family: "Times New Roman", Times, serif;
      }
      button {
        font-size: 25px;
        font-weight: 600;
        padding: 0.2cm 1cm;
        border-radius: 0.2cm;
        background-color: rgb(144, 144, 255);
        position: relative;
        top: 8cm;
        left: 16.3cm;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        border: 1px solid rgb(144, 144, 255);
      }
      button:hover {
        cursor: pointer;
        background-color: rgb(72, 72, 255);
      }
      p {
        margin-top: 9cm;
        width: 12cm;
        margin-left: 13.5cm;
        font-size: 20px;
        height: auto;
        text-align: center;
      }
    </style>
  </head>
  <body>
    <button id="BTN">Generate Data</button>
    <p>
      This is a system I have created which will randomly Generate some data in
      Javascript object format and save it in My MonogoDB collection Employees
      whenever button <b>'GENERATE DATA'</b> is clicked by the User.
    </p>

    <script>
      let Button = document.getElementById("BTN");

      Button.addEventListener("click", async () => {
        let intervalId = setInterval(() => {
          if (Button.innerHTML.endsWith("...")) {
            Button.innerHTML = "Generating Data";
          } else {
            Button.innerHTML += ".";
          }
        },100);

        setTimeout(() => {
          clearInterval(intervalId);
          Button.innerHTML = "Generate Data"; // Reset the text after stopping the interval
        }, 4000);

        console.log("yes! eventlistener is working");
        let a = await fetch("/generate");
        let r = await a.json();
        console.log(r);
      });
    </script>
  </body>
</html>
