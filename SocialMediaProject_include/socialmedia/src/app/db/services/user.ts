import { connectWithMongoDB } from "../dbConnection";
import users from "../models/users";
import { returnDataType } from "../../controllers/user";

export async function userRegistrationService(data:returnDataType) {
    
}